name: Build Windows
on:
  workflow_run:
    workflows: ["Build Linux"]
    types: [completed]
    branches: [main]

jobs:
  build-windows:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Build Release Binary
        run: cargo build --release
      - name: Package Windows Zip
        shell: pwsh
        run: |
          mkdir release
          copy target/release/bzc.exe release/
          Copy-Item -Path runtime -Destination release/runtime -Recurse
          Compress-Archive -Path release/* -DestinationPath bunzo-windows-x86_64.zip
      - uses: actions/upload-artifact@v4
        with:
          name: bunzo-windows
          path: bunzo-windows-x86_64.zip